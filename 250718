# VIBE CODING 이 등장한 세상에서
# 신규 개발자로서 어떠한 자세를 갖추어야 할까!

`git revert`와 `git reset`의 적용 대상은 `commit`된 파일이다.
`add`도 `commit`도 되지 않은 'untracked file'은 `reset`등의 영향을 받지 않는다.

# Attention 메커니즘
에 대해서 충분히 이해하지 못했다.
집 가서 보완하자

# Open AI API
와 관련하여서, temperature과 top-p의 개념에 대해서 배웠다.

온도를 낮추면 창의성이 없고
온도를 높이면 외국어가 혼합되어 나오고
온도를 엄청 높이고 탑피를 엄청 낮추면 비슷비슷한 유형만 추천하고
둘 다 높으면 외계어를 한다.

그 차이점과 이유에 대해 논리적으로 생각하고 정리하자.

